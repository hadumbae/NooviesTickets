import {type BookingErrorCode, BookingErrorCodeSchema, BOOKING_ERROR_CODES} from "@/shared/_schema/strings/BookingErrorCodeSchema";

export {
    BookingErrorCodeSchema,
    BOOKING_ERROR_CODES,
}

export type {
    BookingErrorCode,
}

/**
 * @fileoverview Constant list of all valid lifecycle status values for system users.
 */

/** Tuple array containing the authoritative set of available user statuses. */
export const UserStatusConstant = [
    "ACTIVE",
    "SUSPENDED",
    "INACTIVE",
] as const;
/** @fileoverview Type definition for coordinate form initialization values. */

/** Form values used to initialize the coordinate fields in a form. */
export type CoordinateFormStarterValues = {
    type: any;
    coordinates: [any, any];
};
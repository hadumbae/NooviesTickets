/**
 * @fileoverview Defines types for customizing HTTP error messages.
 */

/** A mapping of HTTP status codes to custom error message strings. */
export type HttpStatusOverrideText = Record<number, string>;

/**
 * @fileoverview Defines the structure for radio input options used with react-hook-form.
 */

/** Represents a single option within a radio button group. */
export type HookRadioOption = {
    label: string;
    value: any;
};

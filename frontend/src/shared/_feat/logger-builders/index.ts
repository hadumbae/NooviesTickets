import {buildFormSubmitLog} from "@/shared/_feat/logger-builders/buildFormSubmitLog.ts";
import {buildContext, LoggerContextKeyData} from "@/shared/_feat/logger-builders/buildLoggerContext.ts";
import {buildStandardLog} from "@/shared/_feat/logger-builders/buildStandardLog.ts";


export {
    buildFormSubmitLog,
    buildContext,
    buildStandardLog,
}

export type {
    LoggerContextKeyData,
}

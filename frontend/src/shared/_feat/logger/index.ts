import {Logger, LoggerFunction} from "@/shared/_feat/logger/Logger.ts";
import {LogPayload, LogType} from "@/shared/_feat/logger/Logger.types.ts";

export {
    Logger,
}

export type {
    LogType,
    LogPayload,
    LoggerFunction,
}


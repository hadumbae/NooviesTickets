import {LocationTarget, LocationTargetSchema} from "@/common/_schemas/strings/location-strings/LocationTargetSchema.ts";
import {TimezoneOffsetSchema, TimezoneOffsetValue} from "@/common/_schemas/strings/location-strings/TimezoneOffsetSchema.ts";

export {
    LocationTargetSchema,
    TimezoneOffsetSchema,
}

export type {
    LocationTarget,
    TimezoneOffsetValue,
}


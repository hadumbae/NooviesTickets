import {buildFormSubmitLog} from "@/common/_feat/logger-builders/buildFormSubmitLog.ts";
import {buildContext, LoggerContextKeyData} from "@/common/_feat/logger-builders/buildLoggerContext.ts";
import {buildStandardLog} from "@/common/_feat/logger-builders/buildStandardLog.ts";


export {
    buildFormSubmitLog,
    buildContext,
    buildStandardLog,
}

export type {
    LoggerContextKeyData,
}

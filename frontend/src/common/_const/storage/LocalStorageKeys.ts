/**
 * @fileoverview Defines constant keys used for browser local storage access.
 */

/** A collection of keys used to persist data in the browser's local storage. */
export const LocalStorageKeys = {
    ipifyCountry: "ipify-country-data",
};
/**
 * @fileoverview Defines constant error codes for invalid context scenarios.
 */

/** List of error codes used when a required context is missing or invalid. */
export const InvalidContextErrorCodeConstant = [
    "missing_context",
    "invalid_values",
    "required_provider",
] as const;
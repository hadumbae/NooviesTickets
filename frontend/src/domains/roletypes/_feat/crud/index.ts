import {RoleTypeCRUDBaseURL} from "@/domains/roletypes/_feat/crud/baseURL.ts";
import {create, destroy, find, findByID, paginated, query, update} from "@/domains/roletypes/_feat/crud/repository.ts";

export {
    RoleTypeCRUDBaseURL,
    find,
    findByID,
    paginated,
    query,
    create,
    update,
    destroy,
}


/**
 * @fileoverview Allowed layout classifications for positions within a theatre's seating grid.
 */

/** Allowed layout classifications for positions within a theatre's seating grid. */
export const SeatLayoutTypeConstant = [
    "SEAT",
    "AISLE",
    "STAIR",
] as const;
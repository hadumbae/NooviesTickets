import {Seat, SeatBaseSchema, SeatSchema} from "@/domains/seats/_schema/model/SeatSchema.ts";
import {SeatDetails, SeatDetailsSchema} from "@/domains/seats/_schema/model/SeatDetailsSchema.ts";

export {
    SeatBaseSchema,
    SeatSchema,
    SeatDetailsSchema,
}

export type {
    Seat,
    SeatDetails,
}


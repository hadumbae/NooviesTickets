import {RoleTypeCRUDBaseURL} from "@/domains/role-types/_feat/crud/baseURL.ts";
import {create, destroy, find, findByID, paginated, query, update} from "@/domains/role-types/_feat/crud/repository.ts";

export {
    RoleTypeCRUDBaseURL,
    find,
    findByID,
    paginated,
    query,
    create,
    update,
    destroy,
}

